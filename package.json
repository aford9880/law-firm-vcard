{
  "name": "site1-legal",
  "version": "1.0.0",
  "description": "Юридическая компания «Правовед» — одностраничный сайт-визитка",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",

    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
